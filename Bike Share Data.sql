SELECT trip_id, duration
	FROM trips
		WHERE duration > 500
			ORDER BY duration desc
	
SELECT * 
	FROM stations 
		WHERE station_id = 84 

SELECT MinTemperatureF 
	FROM weather 
		WHERE events = 'rain' AND zip = 94301

	